import React from "react";
import { Container } from "@mui/material";

const Main = () => {
  return (
    <Container maxWidth="lg" sx={{ border: "2px solid blue" }}>
      Main
    </Container>
  );
};

export default Main;
