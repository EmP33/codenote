import { screen, render } from "@testing-library/react";

describe("<Notes/>", () => {
  it("should switch between newest and most popular on button click", () => {});
  it("should render specific amount of notes", () => {});
});
